# TODO
- [ ] Add Sign Up section/button to `index.html` (standard role only).
- [ ] Add `signup` handler that calls Firebase `createUserWithEmailAndPassword`.
- [ ] Create Firestore `users/{uid}` profile with `role: 'standard'`.
- [ ] Keep existing login flow unchanged.
- [ ] Commit & push to GitHub.
- [ ] Verify on Render that sign-up + login work.

